<custom-component>
  <!-- FULL BG OF NAVBAR -->
  <view class="nav-outer-container fixed" style="background: {{backgroundColor}}">
    
    <!-- TOP BG OF NAVBAR -->
    <view style="height: {{statusBarHeight}}px; background: transparent;"></view>
    
    <!-- BOTTOM BG OF NAVBAR -->
    <view class="nav-container">
      
      <!-- BLACK OVERLAY WITH LANGUAGE CHOICES WHEN LANGUAGE IS CLICKED -->
      <!-- <view wx:if="{{showLanguages}}" bindtap="hideLanguages" class="overlay transparent"></view> -->
      
      <!-- DISPLAY ALL LANGUAGES -->
      <!-- <view class="outer-container column {{shrinkLogo?'transparent':'kill-transparent'}}" bindtap="showLanguages" wx:if="{{!showBackButton && !shared}}">
        <view class="profile-container pos-relative">
          <view class="w100 d-flex">
            <image src="{{lang=='en'?flagEn:flagCn}}" style="width: 45rpx; height: 30rpx;"></image>
            <view style="font-size: 22rpx; margin-left: 14rpx;">{{lang=='en'?'EN':'中文'}}</view>
          </view>

          <view wx:if="{{showLanguages}}" bindtap="selectLanguage" class="profile-container pos-absolute" style="margin-left: 0; top: 80rpx;">
            <image src="{{lang!='en'?flagEn:flagCn}}" style="width: 45rpx; height: 30rpx;"></image>
            <view style="font-size: 22rpx; margin-left: 14rpx;">{{lang!='en'?'EN':'中文'}}</view>
          </view>
        </view>
      </view> -->

      <!-- BACK BUTTON (SAME POSITION AS LANGUAGE) -->
      <view class="outer-container" bindtap="goBack" wx:if="{{showBackButton && !shared}}">
        <view class="back-btn-container column-center">
          <image src="/images/icons/arrow-left.svg" mode="aspectFit" class="btn-img"/>
        </view>
      </view>

      <!-- HOME BUTTON -->
      <view class="outer-container" bindtap="goToHome" wx:if="{{shared}}">
        <view class="back-btn-container icon" style="height: 100%; margin-right: 20rpx;">
          <image src="/images/icons/home.svg" mode="aspectFit" />
        </view>
      </view>

      <!-- LOGO -->
      <!-- <image src="/images/logo.png" mode="aspectFit" class="logo-lg"></image> -->
    </view>
  </view>
  
  <!-- Space that prevent position fixed nav-container from collapsing -->
  <view class="fixed">
    <view style="height: {{statusBarHeight}}px; background: transparent;"></view>
    <view class="nav-space"></view>
  </view>
</custom-component>