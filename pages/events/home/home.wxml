<view style="height: {{statusBarHeight}}px"></view>

<view class="w100 row-center py20 mb50">
  <image src="/images/logo.png" mode="widthFix" style="width: 200rpx;"></image>
</view>

<view class="mb50" wx:if="{{events.viewed.length}}">
  <view class="x-wrapper relative">
    <view class="f36 bold w-100 text-left ter pl20">RECENTLY VIEWED</view>
  </view>
  <!-- <view wx:if="{{past.length==0}}" class="mx20 mt50 mb100 grey f28 text-center">{{t.no_recent}}</view> -->
  <swiper class="card-swiper" bindchange="cardSwiper" style="height: 400rpx;">
    <swiper-item wx:for="{{events.viewed}}" wx:key="index" class="card-wrapper card-small" style="padding-right: 0;">
      <card event="{{item}}" cardType="recent"/>
    </swiper-item>
  </swiper>
</view>

<view class="mb50">
  <view class="x-wrapper relative row-between">
    <view class="f36 bold w-100 text-left ter pl20">ATTENDING</view>
    <!-- <view class="row-around w50 bg-dim pa text-center relative box-b" style="border-radius: 100rpx;" bindtap="changeAttending">
      <view class="absolute bg-sec h100 transition-3" 
        style="{{showUpcomingAtt?'left: 0; right: 50%':'left: 50%; right: 0'}}; border-radius: 100rpx;"></view>
      <view class="w50 px20 py transition-3 {{showUpcomingAtt?'white':'grey-light'}} z1 f24">upcoming</view>
      <view class="w50 px20 py transition-3 {{showUpcomingAtt?'grey-light':'white'}} z1 f24">past</view>
    </view> -->
  </view>

  <card-section events="{{showUpcomingAtt?events.rsvped:events.rsvped_past}}" />

</view>

<view class="mb50">
  <view class="x-wrapper relative row-between">
    <view class="f36 bold w-100 text-left ter pl20">MY EVENTS</view>
    <!-- <view class="row-around w50 bg-dim pa text-center relative box-b" style="border-radius: 100rpx;" bindtap="changeMine">
      <view class="absolute bg-sec h100 transition-3" 
        style="{{showUpcomingMine?'left: 0; right: 50%':'left: 50%; right: 0'}}; border-radius: 100rpx;"></view>
      <view class="w50 px20 py transition-3 {{showUpcomingMine?'white':'grey-light'}} z1 f24">upcoming</view>
      <view class="w50 px20 py transition-3 {{showUpcomingMine?'grey-light':'white'}} z1 f24">past</view>
    </view> -->
  </view>

  <card-section events="{{showUpcomingMine?events.created:events.created_past}}" />

  <!-- <view wx:if="{{!events.created.length}}" class="mx20 mt50 mb100 grey f28 text-left pl20">No event created. Create your first event!</view>
  <swiper wx:if="{{events.created.length}}" class="card-swiper" bindchange="cardSwiper" style="height: 700rpx;">
    <swiper-item wx:for="{{events.created}}" wx:key="index" class="card-wrapper card-big">
      <card event="{{item}}"/>
    </swiper-item>
  </swiper> -->
</view>

<view class="column-center">
  <view class="row-center">
    <view class="f24 grey bold">Made with</view>
    <image src="/images/icons/love.svg" mode="aspectFit" class="icon-mini mx"></image>
    <view class="f24 grey bold">by</view>
  </view>
  <image src="/images/codepanda.png" mode="aspectFit" style="width: 350rpx; height: 350rpx;"></image>
</view>

<navigator url="/pages/events/create/create" class="fixed bg-sec f48 white row-center z9" 
  style="bottom: 100rpx; right: 60rpx; border-radius: 50%; height: 100rpx; width: 100rpx; box-shadow: 0px 5px 30px rgba(255, 201, 60, 0.7);">+</navigator>
